---
export const prerender = false;

import { getLangFromUrl, useTranslatedPath } from "@/i18n/utils";

const redirectedUrl = useTranslatedPath(getLangFromUrl(Astro.url))(
  `/communities/${Astro.params.id ?? ""}`,
);

Astro.redirect(redirectedUrl, 301);
---
