---
const { title, class: className } = Astro.props;
---

<section
  class:list={[
    "mx-auto mb-14 mt-0 w-full max-w-3xl max-[700px]:mb-9",
    className,
  ]}
>
  {title && <h2 class="mb-2 text-2xl font-bold">{title}</h2>}
  <slot />
</section>
